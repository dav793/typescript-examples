{
  "name": "typescrpt-examples-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "tsc && concurrently --kill-others \"tsc --watch\" \"nodemon -L --polling-interval 1000 dist/main.js\"",
    "serve": "tsc && node dist/main.js",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "David Vargas (dav793@gmail.com)",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.11.17",
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.3"
  }
}
